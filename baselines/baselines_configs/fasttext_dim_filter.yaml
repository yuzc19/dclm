- source: cc
  steps:
    - func: classify_fasttext_hq_prob_enricher
      model_filename: oracle-40000.bin  # Change this to the name of your model file
      key: fasttext_dim_hq_prob         # Change this to the name of the desired key
      lq_label: __label__lq
    - func: quality_filter
      key: fasttext_dim_hq_prob         # Make sure this matches with the key from the enricher
      threshold: 0.58                   # Change this to your chosen threshold. 0.53 (60%) 0.54 (50%) 0.56 (25%) 0.58 (10%)
